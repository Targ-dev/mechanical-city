# mechanical-city

Starter Next.js + TypeScript + Tailwind e-commerce app skeleton.

# Development

1. Copy `.env.local.example` to `.env.local` and set values.
2. Install dependencies: `npm install`
3. Run dev: `npm run dev`
